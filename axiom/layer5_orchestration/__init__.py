# OCIF Layer 5: Intelligence Orchestration Layer (Agent Runtime)
# Handles dynamic task planning, tool discovery, collaborative agent execution,
# prompt template rendering, and side-effect gating (ProposalReady).
