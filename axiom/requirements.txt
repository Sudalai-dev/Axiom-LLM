fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
sqlalchemy==2.0.31
alembic>=1.13.0         # schema migrations (alembic upgrade head) — SQLite & PostgreSQL
pydantic-settings==2.3.4
python-multipart==0.0.9
aiosqlite>=0.20.0
httpx==0.28.1
python-dotenv>=1.0.0
cryptography>=42.0.0    # Fernet encryption of per-user provider API keys (core/security.py)

# Multimodal Project Understanding (Phase 6) — real parsers for uploaded
# project files. Every call site catches ImportError and degrades to an
# empty extraction (never crashes) if one of these is missing, matching the
# existing fail-soft pattern in datasets/ingestion.py.
PyMuPDF>=1.24.0        # PDF text extraction (imported as `fitz`)
python-docx>=1.1.0     # DOCX text extraction
python-pptx>=0.6.23    # PPTX text extraction
openpyxl>=3.1.0        # XLSX text extraction
Pillow>=10.0.0         # Image metadata (dimensions/format) — no OCR/vision

# PostgreSQL drivers — required only when AXIOM_DATABASE_URL points at Postgres
# (both are needed: asyncpg for the app, psycopg2 for Alembic). Without a
# Postgres URL, AXIOM uses the bundled SQLite and neither is imported.
# asyncpg>=0.29.0
# psycopg2-binary==2.9.9

# Optional infra extras — the platform runs fully offline without these
# (in-memory rate limiting / vector store fallbacks activate automatically)
# redis==5.0.7
# aiokafka==0.11.0

# Optional ML/RAG extras for advanced features
# sentence-transformers==3.0.1
# chromadb==0.5.3
