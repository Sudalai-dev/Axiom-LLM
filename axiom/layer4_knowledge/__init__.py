# OCIF Layer 4: Knowledge Enrichment Service (RAG)
# Handles document parsing, semantic chunking, embedding generation,
# hybrid lexical-vector search, RRF fusion, citation metadata,
# and Knowledge Graph entities relation traversal.
